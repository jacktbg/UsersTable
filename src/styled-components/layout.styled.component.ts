import styled from "styled-components"

export const LayoutContainer = styled.div`
  margin-top: clamp(50px, 3%, 100px);
  padding: 0px 10px;
  @media only screen and (max-width: 1176px) {
    padding: 12px;
  }
`
